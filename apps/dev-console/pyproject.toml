[project]
name = "dev-console"
version = "0.1.0"
description = "Local dev-service control console (stdlib only)"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
dev-console = "server:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["server"]
