[project]
name = "profile"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["fastapi>=0.100", "uvicorn>=0.20"]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[dependency-groups]
dev = [
    "httpx2>=2.0.0",
]
