# Python bytecode / packaging
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments & tool caches
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
tmp_pytest/

# Runtime data (DBs, logs, exports) — only the README is tracked
data/*
!data/README.md
apps/*/data/

# gacore run_repl 启动即导出的图（test_cli 每个用例都会在 CWD 落一份）
agent_graph.png

# withlanggraph 测试用相对路径 file_write 落盘的产物（test_qq/test_middleware，
# 上游自带 .gitignore 只覆盖其子目录，测试 CWD 是仓库根）
h.txt
notes.txt

# withlanggraph 服务启动日志（mono 切换后 Start-Process 重定向落盘于应用根）
apps/withlanggraph/gacore.log
apps/withlanggraph/gacore.log.err

# Secrets
.env
.env.*
!.env.example

# IDE / OS
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store
